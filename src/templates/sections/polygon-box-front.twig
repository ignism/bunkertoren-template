{% extends './polygon-box.twig' %}

{% block content %}
    <div class="content push-down">
        <h2>{{ text }}</h2>
    </div>

    <div class="level">
        <div class="left-item level-left">
            <p>
                <a href="mailto:{{ mail }}">{{ mail }}</a><br><a href="tel:{{ phone|replace({' ': ''}) }}">{{ phone }}</a>
            </p>
        </div>
        <div class="left-item level-right">
            <a href="#" class="button">schrijf je in</a>
        </div>
    </div>
{% endblock %}