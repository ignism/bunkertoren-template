{% extends './section.twig' %}

{% set section_classes = ['text-360', 'fit-height'] %}

{% block content %}

    {% if orientation == 'text-left' %}
        {% include './partials/text-360-left.twig' with {
            'clipping': 'is-clipped-right'
        } %}
    {% else %}
        {% include './partials/text-360-right.twig' with {
            'clipping': 'is-clipped-left'
        } %}
    {% endif %}

{% endblock %}